# Getting Started

To set up the environment on Linux:

```
chmod +x setup.sh
./ setup.sh
```
This will update everything, create a virtual environment, and download all dependencies

I plan to use different branches for differerent chapters.